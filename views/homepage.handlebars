<h1>Upcoming Games</h1>
<ul class="game-list">
  {{#each events}}
    <li>
      <a href="/event/{{ this.id }}">
        <div class="game-details">
          <img src="{{ this.teamLogo }}" alt="{{ this.title }} logo" class="team-logo" />
          <p>{{ this.date }}</p>
          <p>{{ this.attendees.length }} attendees</p>
        </div>
      </a>
    </li>
  {{/each}}
</ul>
